apiVersion: v1
kind: Service
metadata:
  name: inference
  labels:
    app: inference
spec:
  type: ClusterIP # Default, but explicit for clarity
  selector:
    app: inference
  ports:
  - name: inference-api
    port: 8080
    targetPort: 8080 